services:
    pn.forum.manager.notify:
        class: ProjetNormandie\ForumBundle\Manager\NotifyManager
        arguments:
            - '@doctrine.orm.entity_manager'
            - '@translator'
            - '@pn.message.builder.message'

